<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.sereus.health">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Assistant camera capture (react-native-image-picker). Camera is optional. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Reminder notifications (notifee). POST_NOTIFICATIONS is the Android 13+ runtime
         permission. notifee's AAR injects SCHEDULE_EXACT_ALARM; we schedule only inexact
         (SET_AND_ALLOW_WHILE_IDLE) alarms, so strip it to satisfy Google Play's policy. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Custom scheme: in-app / notification / scenario deep links
             (health://screen/Route?variant=...). Any app can claim this, so it's
             for app-to-app + notifications, not trusted web links. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="health" />
        </intent-filter>
        <!-- Verified Android App Links: https://health.sereus.org/... opens the app
             once /.well-known/assetlinks.json (with this app's signing SHA-256) is
             served from that host. autoVerify is inert until then (links just open in
             the browser) — safe to ship ahead of the server side. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="health.sereus.org" />
        </intent-filter>
      </activity>
    </application>
</manifest>
